<div class="accordion">
  <slot />
</div>

<style>
  .accordion {
    border: 1px solid var(--border);
    border-radius: var(--radius);
  }

  .accordion > :global(* + *) {
    border-top: 1px solid var(--border);
  }
</style>
